
  <!doctype html>
    <html>
  <head>
    <link rel="stylesheet" href="{{ 'vihanga-page.css' | asset_url }}" type="text/css" />
  </head>
  <body>
  
  <header class="page-header">
    <div class="header-container">
      <div class="header-left">
        <img src="{{ 'logoPic.png' | asset_url }}" alt="Gift Guide" class="Logo-nav" width="" height="">
      </div>
      <div class="header-center">Find the ideal gift for your loved ones.</div>
      <div class="header-right">
        <div>
          <a href="#" class="header-button-nav">
            <span class="bttn-txt-header-nav">Shop Now </span>
            <span class="arrow-header-nav">→</span>
          </a>
        </div>
      </div>
      <!-- Mobile Header -->
    </div>
    <div class="mobile-header">
      <div class="mobile-header-left">
        <div class="burger-icon" onclick="toggleMenu()">
          &#9776;
          <!-- Unicode character for burger icon -->
        </div>
      </div>
      <div class="mobile-header-center">
        <img src="{{ 'logoPic.png' | asset_url }}" alt="Gift Guide" class="Logo-nav" width="" height="">
      </div>
    </div>

    <!-- Overlay Menu -->
    <div id="overlayMenu" class="overlay">
      <div class="overlay-content">
        <p class="mobile-heder-text">Find the ideal gift for your loved ones.</p>
        <div>
          <a href="#" class="header-button-nav">
            <span class="bttn-txt-header-nav">Shop Now </span>
            <span class="arrow-header-nav">→</span>
          </a>
        </div>
      </div>
    </div>
  </header>
  <div class="Main-container">
    <div class="simple-banner-section">
      <div class="banner-image" style="background-image: url('{{ 'banner-imageBG.png' | asset_url }}');">
        <div class="text-overlay">
          <div>
            <div class="hero-title">The Gift Guide</div>
            <div class="hero-para">
              Discover Joy: Your Ultimate Holiday Gift Destination. Explore our curated selection and find the perfect
              gifts to delight your loved ones this holiday season.
            </div>
          </div>
          <div>
            <a href="#" class="cta-button">
              <span class="bttn-txt">Shop Now </span>
              <span class="arrow">→</span>
            </a>
          </div>
        </div>
      </div>


      
      
      <!-- Section for small devices -->
      <div class="small-device-sec">
        <h2 class="small-device-title">The Gift Guide</h2>
        <h2 class="small-device-subtitle">Discover Joy: Your Ultimate Holiday Gift Destination.</h2>

        <div class="mobile-banner-wrapper">
          <a href="#" class="cta-button mobile-banner-button">
            <span class="bttn-txt">Shop Now </span>
            <span class="arrow">→</span>
          </a>
          <img src="{{ 'MobleBanner.png' | asset_url }}" alt="Gift Guide" class="small-device-image" width="" height="">
        </div>
      </div>
    </div>
    <div class="hero-end-part">
      <span class="lg-end-text">SUSTAINABLE, ETHICALLY MADE CLOTHES IN SIZES XXS TO 6XL</span>
      <span class="sm-end-text">SUSTAINABLE, ETHICALLY MADE ACTIVEWEAR</span>
    </div>

    <div class="Grid-container">
      <div class="grid-title">Tisso vison in the wild</div>
      <div class="product-grid">
        <div class="product-block" data-product-id="1">
          <img src="{{ 'bag.webp' | asset_url }}" alt="Gift" class="Card-img" width="" height="">
          <button class="view-details">+</button>
        </div>
        <div class="product-block" data-product-id="1">
          <img src="{{ 'Blue.webp' | asset_url }}" alt="Gift" class="Card-img" width="" height="">
          <button class="view-details">+</button>
        </div>
        <div class="product-block" data-product-id="1">
          <img src="{{ 'redja.webp' | asset_url }}" alt="Gift" class="Card-img" width="" height="">
          <button class="view-details">+</button>
        </div>
        <div class="product-block" data-product-id="1">
          <img src="{{ 'coffee.webp' | asset_url }}" alt="Gift" class="Card-img" width="" height="">
          <button class="view-details">+</button>
        </div>
        <div class="product-block" data-product-id="5">
          <img src="{{ 'redpoket.webp' | asset_url }}" alt="Gift" class="Card-img" width="" height="">
          <button class="view-details">+</button>
        </div>
        <div class="product-block" data-product-id="6">
          <img src="{{ 'denim.webp' | asset_url }}" width="" height="" alt="Gift" class="Card-img">
          <button class="view-details">+</button>
        </div>
      </div>

      <!-- Popup for product details -->
      <div class="product-popup">
        <div class="popup-content">
          <span class="close-popup" onclick="closePopup()">×</span>
          <div class="popup-header">
            <div class="popup-image-cont">
              <img class="product-image" src="" alt="" width="" height="">
            </div>
            <div class="popup-info">
              <span class="product-name"></span>
              <span class="product-price"></span>
              <div class="product-description"></div>
            </div>
          </div>
          <div class="popup-details">
            <span class="size-color-txt">Colors</span>
            <div class="product-colors"></div>
            <span class="size-color-txt">Size</span>
            <div class="custom-dropdown">
              <div class="dropdown-header">
                <div class="dropdown-content" onclick="toggleDropdown()">
                  <div id="selected-option" class="size-color-txt">Choose your size</div>
                  <div class="divider"></div>
                  <div class="arrow-container">
                  <div id="dropdown-arrow" class="arrow">  <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="black" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <polyline points="6 9 12 15 18 9"></polyline>
                  </svg></div>
                </div>
                </div>
              </div>
              <div id="dropdown-menu" class="dropdown-menu">
                <div class="dropdown-item" onclick="selectOption('XS')">XS</div>
                <div class="dropdown-item" onclick="selectOption('S')">S</div>
                <div class="dropdown-item" onclick="selectOption('M')">M</div>
              </div>
            </div>
            
            

            <div>
              <a href="#" class="popup-bttn" onclick="addToCart(event)" >
                <span class="bttn-txt">ADD TO CART </span>
                <span class="arrow">→</span>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
</html>

  <script>
    const products = [
      {
        id: 1,
        name: 'Orange Wide Leg',
        price: 19.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "bag.webp" | asset_url }}',
        colors: ['White', 'Black'],
        sizes: ['Medium', 'Large'],
      },
      {
        id: 2,
        name: 'Tailored Jacket',
        price: 29.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "Blue.webp" | asset_url }}',
        colors: ['Blue', 'Black'],
        sizes: ['Medium', 'Large'],
      },
      {
        id: 3,
        name: 'Accordion Pleated Dress',
        price: 39.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "redja.webp" | asset_url }}',
        colors: ['Red', 'Yellow'],
        sizes: ['Medium', 'Large'],
      },
      {
        id: 4,
        name: 'Green Trench Coat',
        price: 49.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "coffee.webp" | asset_url }}',
        colors: ['White', 'Black'],
        sizes: ['Medium', 'Large'],
      },
      {
        id: 5,
        name: 'Tennis Blue T-Shirt',
        price: 59.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "redpoket.webp" | asset_url }}',
        colors: ['Grey', 'Black'],
        sizes: ['Medium', 'Large'],
      },
      {
        id: 6,
        name: 'Long Sleeve Tennis Top',
        price: 69.99,
        description:
          'This one-piece swimsuit is crafted from jersey featuring an allover micro Monogram motif in relief.',
        image: '{{ "denim.webp" | asset_url }}',
        colors: ['Blue', 'Black'],
        sizes: ['Medium', 'Large'],
      },
    ];
    let selectedColor = '';
    let selectedSize = '';

    function toggleMenu() {
      const overlay = document.getElementById('overlayMenu');
      const burgerIcon = document.querySelector('.burger-icon');

      if (overlay.style.display === 'block') {
        overlay.style.display = 'none';
        burgerIcon.innerHTML = '&#9776;'; // Burger icon
      } else {
        overlay.style.display = 'block';
        burgerIcon.innerHTML = 'X'; // Close icon
      }
    }

    function renderProductGrid() {
      const gridContainer = document.querySelector('.product-grid');
      gridContainer.innerHTML = '';

      products.forEach((product) => {
        const productBlock = document.createElement('div');
        productBlock.className = 'product-block';
        productBlock.setAttribute('data-product-id', product.id);
        productBlock.innerHTML = `
          <img src="${product.image}" alt="${product.name}" class="Card-img">
          <button class="view-details" onclick="showProductPopup(${product.id})">+</button>
        `;
        gridContainer.appendChild(productBlock);
      });
    }

    function showProductPopup(productId) {
      const product = products.find((p) => p.id === productId);
      if (!product) {
        console.error('Product not found for ID:', productId);
        return;
      }

      const popup = document.querySelector('.product-popup');
      if (!popup) {
        console.error('Popup element not found');
        return;
      }

      const productImage = popup.querySelector('.product-image');
      const productName = popup.querySelector('.product-name');
      const productPrice = popup.querySelector('.product-price');
      const productDescription = popup.querySelector('.product-description');
      const productColors = popup.querySelector('.product-colors');


      // Update popup content with selected product details
      productImage.src = product.image;
      productName.textContent = product.name;
      productPrice.textContent = `$${product.price.toFixed(2)}`;
      productDescription.textContent = product.description;

      // Populate color options
      renderColorSwatches(product.colors);

      // Populate size options
      
      console.log('Popup content set for product:', product.name);
      popup.style.display = 'flex';
    }

    function renderColorSwatches(colors) {
      const productColors = document.querySelector('.product-colors');
      productColors.innerHTML = ''; // Clear existing colors

      colors.forEach((color) => {
        const colorOption = document.createElement('div');
        colorOption.className = 'color-option';
        colorOption.style.border = '1px solid black';
        colorOption.style.borderLeft = `7px solid ${color}`;
        colorOption.style.padding = '5px';
        colorOption.style.margin = '5px 0';
        colorOption.style.width = '130px';
        colorOption.textContent = color;

        // Add click event to handle selection
        colorOption.addEventListener('click', () => {
          // Remove 'selected' class from all options
          const allOptions = document.querySelectorAll('.color-option');
          allOptions.forEach((option) => option.classList.remove('selected'));

          // Add 'selected' class to the clicked option
          colorOption.classList.add('selected');

          // Get the selected color value
          const selectedColor = colorOption.textContent;
          console.log('Selected Color:', selectedColor); // You can handle this value as needed
        });

        productColors.appendChild(colorOption);
      });
    }

    function closePopup() {
      const popup = document.querySelector('.product-popup');
      if (popup) {
        popup.style.display = 'none';
      }
    }

    // Event listener to close popup on background click or close button
    document.querySelector('.product-popup').addEventListener('click', (e) => {
      console.log('Click event detected on popup:', e.target);
      if (e.target.classList.contains('product-popup') || e.target.classList.contains('close-popup')) {
        closePopup();
      }
    });

    // Render the product grid on DOMContentLoaded
    document.addEventListener('DOMContentLoaded', () => {
      console.log('Document loaded, rendering product grid.');
      renderProductGrid();
    });
    // Function to toggle the dropdown menu
// Function to toggle the dropdown menu
// Function to toggle the dropdown menu
// Function to toggle the dropdown menu
function toggleDropdown() {
  const dropdownMenu = document.getElementById("dropdown-menu");
  const dropdownArrow = document.getElementById("dropdown-arrow");

  if (dropdownMenu.style.display === "block") {
    dropdownMenu.style.display = "none";
    dropdownArrow.style.transform = "rotate(0deg)"; // Reset arrow to default
  } else {
    dropdownMenu.style.display = "block";
    dropdownArrow.style.transform = "rotate(180deg)"; // Rotate arrow when open
  }
}

// Function to handle option selection
function selectOption(option) {
  document.getElementById("selected-option").innerText = option;
  toggleDropdown(); // Close the dropdown after selecting an option
}
 function selectColor(color) {
      selectedColor = color; // Save selected color
      // Add additional logic here if needed
    }
// Close the dropdown if clicked outside
window.onclick = function(event) {
  if (!event.target.matches('.dropdown-header') && !event.target.closest(".custom-dropdown")) {
    const dropdowns = document.getElementsByClassName("dropdown-menu");
    const dropdownArrow = document.getElementById("dropdown-arrow");
    for (let i = 0; i < dropdowns.length; i++) {
      const openDropdown = dropdowns[i];
      if (openDropdown.style.display === "block") {
        openDropdown.style.display = "none";
        dropdownArrow.style.transform = "rotate(0deg)";
      }
    }
  }
};

function addToCart(event) {
  event.preventDefault(); // Prevent default anchor behavior

  if (!selectedColor || !selectedSize) {
    alert('Please select a color and size before adding to cart.');
    return;
  }

  // Add the selected product to the cart
  console.log(`Added to cart: ${selectedColor}, ${selectedSize}`);

  // Check for automatic addition of the "Soft Winter Jacket"
  if (selectedColor === 'Black' && selectedSize === 'Medium') {
    console.log('Automatically adding Soft Winter Jacket to the cart.');

    // Define the Soft Winter Jacket details
    const softWinterJacket = {
      id: 'soft_winter_jacket', // Unique ID for the Soft Winter Jacket
      name: 'Soft Winter Jacket',
      price: 79.99, // Example price
      description: 'A warm and comfortable winter jacket.',
      image: '{{ "soft_winter_jacket.webp" | asset_url }}', // Image URL
      color: 'Black',
      size: 'Medium',
    };

    // Here you can add code to add the Soft Winter Jacket to the cart
    console.log(`Added to cart: ${softWinterJacket.name}, Color: ${softWinterJacket.color}, Size: ${softWinterJacket.size}`);
  }

  // Implement the logic for adding the selected product to the cart
  // This could involve updating a cart object, sending a request to a server, etc.
  console.log(`Product added to cart: ${selectedColor}, ${selectedSize}`);
}


  </script>


